<% content_for :title do %>
    Nieuw recept
<% end %>

<div>
  <%= form_for @recipe, url: recipe_index_path do |f| %>
    <% if @recipe.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@recipe.errors.count, 'error') %> prohibited this meeting from being saved:</h2>

          <ul>
            <% @recipe.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name %><br>
      <%= f.text_field :name, class: 'form-control', required: true, minlength: 2 %>
    </div>
    <div class="form-group">
      <%= f.label :beer %><br>
      <%= f.text_field :beer, class: 'form-control', required: true, minlength: 2 %>
    </div>
    <div class="form-group">
      <%= f.rich_text_area :actiontext_description, class: 'form-control' %>
    </div>
    <div class="actions">
      <%= f.submit :Verstuur, class: 'btn btn-primary' %>
    </div>
<% end %>
</div>
<%= link_to 'Terug', recipe_index_path, class: 'btn btn-default' %>
