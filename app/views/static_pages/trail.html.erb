<% content_for :title do %>
  Trail
<% end %>
<script type="text/javascript">
$(document).ready(function()
    {
      $("#motion").tablesorter();
    }
    );
</script>
<table id="motion" class="tablesorter table table-hover">
  <thead>
    <tr>
      <th>Wie</th>
      <th>Wat</th>
      <th>Waneer</th>
      <th>Hoe</th>
    </tr>
  </thead>
  <%= will_paginate @trail %>
  <tbody>
    <% @trail.each do |t| %>
      <tr>
        <td><% if t.whodunnit %>
          <%= User.find(t.whodunnit).name %>
        <% else %>
          System </td>
      <% end %>
      <% if t.event == "create" %>
        <td>created <%= t.item_type %></td>
      <% elsif t.event == "destroy"%>
        <td>destroyed <%= t.item_type + " #" + t.item_id.to_s %></td>
      <% else %>
      <td><%= t.event %>d <%= t.item_type + " #" + t.item_id.to_s %></td>
      <% end %>
      <td><%= time_ago_in_words(t.created_at) %> geleden.</td>
      <td><%= t.changeset %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @trail %>
