<%= form_for(@public_page) do |f| %>
  <% if @public_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_page.errors.count, "error") %> prohibited this public_page from being saved:</h2>

      <ul>
      <% @public_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, rows: 25 %>
  </div>
  <div class="field">
    <%= f.label :public %><br>
    <%= f.check_box :public %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
