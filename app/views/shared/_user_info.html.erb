<%= link_to gravatar_for(current_user, size: 96), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Mijn profiel", current_user %>
</span>
<span>
  <%= pluralize(current_user.quotes.count, "quote") %>
</span>
