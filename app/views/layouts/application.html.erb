<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Hamers zonder Sikkel</title>

    <!-- Favicon settings -->
    <link rel="apple-touch-icon" sizes="180x180" href="/img/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/img/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/img/favicon-16x16.png">
    <link rel="manifest" href="/img/site.webmanifest">
    <link rel="shortcut icon" href="/img/favicon.ico">

    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %>

    <%= csrf_meta_tags %>
  </head>
  <body class="h-full">
    <div class="bg-gray-100 min-h-screen h-full">
      <div class="bg-gray-900 pb-32">
        <%= render 'layouts/nav' %>

        <% if content_for?(:title) %>
          <header class="py-8">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
              <div class="flex flex-wrap">
                <div class="flex-auto mr-2">
                  <%= render 'shared/breadcrumbs' %>
                  <h1 class="text-3xl font-bold text-white">
                    <%= yield :title %>
                  </h1>
                  <% if content_for?(:subtitle) %>
                    <div class="flex flex-col my-2 sm:flex-row sm:flex-wrap sm:space-x-6">
                      <%= yield :subtitle %>
                    </div>
                  <% end %>
                </div>
                <div class="flex-row-reverse place-self-center">
                  <%= yield :options %>
                </div>
              </div>
            </div>
          </header>
        <% end %>
      </div>

      <main class="-mt-32">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
          <div class="bg-white rounded-lg overflow-hidden shadow p-6">
            <%= yield %>
          </div>
        </div>
      </main>

      <%= render "layouts/footer" %>
    </div>
    <%= turbo_frame_tag "flash" do %>
      <%= render 'layouts/alert' %>
    <% end %>
  </body>
</html>
