<% content_for :title do %>
  Nieuwe review
<% end %>

<%= form_for @beer.reviews.build(user_id: current_user.id, beer_id: @beer.id), html: {class: 'form-horizontal'} do |f| %>
  <%= render partial: 'reviews/form', locals: {f: f} %>
<% end %>
