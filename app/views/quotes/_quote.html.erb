<li id="quote_<%= quote.id %>">
  <%= link_to gravatar_for(quote.user), quote.user %>
  <span class="user">
    <%= link_to quote.user.name, quote.user %>
  </span>
  <span class="content">
    <%= quote.text %>
  </span>
  <span class="text-muted">
    <i class="far fa-clock"></i> <%= time_ago_in_words(quote.created_at) %> geleden.
  </span>
  <div class="btn-group">
    <%= link_to edit_quote_path(quote),
      class: 'btn btn-default btn-sm' do %>
      <i class="fas fa-pencil-alt"></i>
    <% end %>
    <%= link_to quote,
      method: :delete,
      data: {confirm: 'Weet je het zeker?'},
      title: quote.text,
      class: 'btn btn-default btn-sm' do %>
      <i class="far fa-trash-alt"></i>
    <% end %>
  </div>
</li>
