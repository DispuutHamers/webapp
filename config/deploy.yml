<% require "dotenv"; Dotenv.load(".env") %>

service: hamers-webapp
image: dispuuthamers/webapp

servers:
  web:
    - 130.89.148.88

proxy:
  ssl: true
  host: vps.zondersikkel.nl
  app_port: 3000
  response_timeout: 60
  healthcheck:
    path: /
    timeout: 60

registry:
  server: ghcr.io
  username:
    - KAMAL_REGISTRY_USERNAME
  password:
    - KAMAL_REGISTRY_PASSWORD

builder:
  arch: amd64

env:
  clear:
    RAILS_ENV: production
    RAILS_LOG_TO_STDOUT: true
  secret:
    - RAILS_MASTER_KEY

aliases:
  console: app exec --interactive --reuse "rails console"
  shell: app exec --interactive --reuse "bash"
  db: app exec --interactive --reuse "rails dbconsole"

volumes:
 - db:/webapp/db
 - log:/webapp/log # includes cron logs
 - storage:/webapp/storage
